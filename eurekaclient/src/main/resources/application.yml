server:
  port: 8010
spring:
  application:
    name: provider
eureka:
  client:
    service-url:
#      注册中心的地址
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true